// "DOMContentLoaded" espera o HTML ser carregado.
document.addEventListener('DOMContentLoaded', (event) => {
    
    // O código da navbar (openBtn) foi removido
    // porque ela agora funciona 100% com CSS (:hover).

    // Você pode adicionar aqui futuros códigos JS,
    // como funções para o mapa ou para o chatbot.
    console.log("Página carregada e pronta!");

});